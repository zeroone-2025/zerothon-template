# 🛠️ 개발 문서

> **Claude Code 활용 안내**: `@docs/01_plan.md`를 참조하면 기획 문서의 프로젝트 개요와 MVP 범위를 자동으로 확인할 수 있습니다. 아래 프롬프트를 참고하여 Claude Code에서 자연어로 요청하세요.
>
> **기획자가 확정한 MVP 범위를 벗어나지 않도록 주의하세요.** 범위 변경이 필요하면 기획자와 먼저 논의하세요.

## 기술 스택

| 분류 | 기술 | 용도 |
|------|------|------|
| 프레임워크 | Next.js 15 (App Router) | 풀스택 웹 앱 |
| UI | shadcn/ui + Tailwind CSS | 컴포넌트 & 스타일링 |
| 상태 관리 | Zustand | 클라이언트 상태 |
| 서버 상태 | React Query | API 데이터 캐싱 |
| (추가) | | |

> **AI 프롬프트** -- 추가 라이브러리 선정
>
> ```
> @docs/01_plan.md 참고해서, 핵심 기능 구현에 필요한 추가 라이브러리를 추천해줘.
> 기본 스택: Next.js 15 (App Router), TypeScript, Tailwind CSS v4, shadcn/ui, Zustand, React Query
> 각 기능별 라이브러리, 설치 명령어, 선택 이유(번들 크기/러닝커브)를 포함해줘.
> 2시간 해커톤이라 설정 간단하고 바로 쓸 수 있는 것 위주로.
> ```
>
> **TIP**: 라이브러리는 최소한으로! 하나 추가할 때마다 설정 시간이 늘어납니다.

## 화면 구성

| 페이지 | 경로 | 설명 |
|--------|------|------|
| 메인 | `/` | 랜딩 페이지 |
| (추가) | | |

> **AI 프롬프트** -- App Router 페이지 설계
>
> ```
> @docs/01_plan.md 참고해서, 이 서비스에 필요한 페이지를 설계해줘.
> Next.js 15 App Router 기준으로 페이지 목록(경로, 설명, 주요 UI 요소),
> 파일 경로, 네비게이션 흐름, 동적 라우트를 표 형식으로 정리해줘.
> ```
>
> **TIP**: MVP에서는 3-4페이지면 충분합니다. 메인 + 핵심 기능 1-2개 + 결과 페이지 정도로 구성하세요.
>
> **TIP**: 화면 구성과 API 설계 결과를 기획자와 한 번 맞춰보세요. 기획 의도와 다르면 나중에 수정이 커집니다.

## 컴포넌트 구조

```
src/
├── app/              # 페이지 & 라우트
├── components/
│   ├── ui/           # shadcn/ui 컴포넌트
│   └── layout/       # 레이아웃 컴포넌트 (header, footer)
├── lib/              # 유틸리티
├── hooks/            # 커스텀 훅
├── stores/           # Zustand 스토어
└── providers/        # React Query 등 Provider
```

> **AI 프롬프트** -- 컴포넌트 트리 설계
>
> ```
> @docs/01_plan.md 와 위 화면 구성을 참고해서, 각 페이지별 컴포넌트 트리를 설계해줘.
> shadcn/ui (Button, Card, Input, Dialog, Sheet, Tabs 등)를 활용하고,
> 재사용 가능한 공통 컴포넌트, shadcn 설치 명령어, 트리 구조를 포함해줘.
> ```
>
> **TIP**: shadcn/ui 컴포넌트를 최대한 활용하면 시간을 아낄 수 있습니다. `npx shadcn@latest add button card input` 식으로 한꺼번에 추가하세요.

## API 설계

| Method | Endpoint | 설명 | 요청 Body | 응답 |
|--------|----------|------|-----------|------|
| GET | `/api/hello` | 테스트 API | - | `{ message, timestamp }` |
| (추가) | | | | |

> **AI 프롬프트** -- Route Handler 설계
>
> ```
> @docs/01_plan.md 참고해서, 필요한 API 엔드포인트를 설계해줘.
> Next.js 15 App Router Route Handler 형식으로, 각 기능별 API(Method, Endpoint, 설명, 요청/응답),
> 파일 경로, 에러 처리 형식을 표로 정리해줘.
> 외부 DB 없이 인메모리/로컬 파일로 데이터를 관리하는 방식으로.
> ```
>
> **TIP**: 해커톤에서는 외부 DB 연동보다 인메모리 데이터나 로컬 JSON이 빠릅니다.

## 코드 생성 가이드 & 프롬프트 기록

> Claude Code에서 아래 프롬프트를 참고하여 코드를 생성하세요. 사용한 프롬프트는 하단 기록란에 남겨두면 심사 시 가산점이 됩니다.

### 페이지 생성 프롬프트

> **AI 프롬프트** -- 새 페이지 생성
>
> ```
> src/app/{{페이지명}}/page.tsx 파일을 만들어줘.
> 역할: {{이 페이지가 하는 일}}
> 주요 UI: {{필요한 UI 요소 나열}}
> TypeScript + Tailwind CSS + shadcn/ui 사용하고, 반응형으로 만들어줘.
> ```
>
> **TIP**: 페이지 설명을 구체적으로 쓸수록 결과물의 완성도가 올라갑니다.

### API 생성 프롬프트

> **AI 프롬프트** -- 새 API Route Handler 생성
>
> ```
> src/app/api/{{엔드포인트}}/route.ts 파일을 만들어줘.
> 기능: {{이 API가 하는 일}}
> Method: {{GET / POST / PUT / DELETE}}
> 요청: {{요청 Body 구조}}
> 응답: {{응답 데이터 구조}}
> NextResponse 사용하고 에러 처리, TypeScript 타입 정의 포함해줘.
> ```
>
> **TIP**: 위 API 설계 섹션의 스펙을 참고하면 됩니다.

### 컴포넌트 생성 프롬프트

> **AI 프롬프트** -- 새 컴포넌트 생성
>
> ```
> src/components/{{컴포넌트명}}.tsx 파일을 만들어줘.
> 역할: {{이 컴포넌트가 하는 일}}
> Props: {{받아야 할 props}}
> shadcn/ui 컴포넌트: {{Button, Card, Input 등}}
> TypeScript + Tailwind CSS로, 필요하면 Zustand 상태 관리도 포함해줘.
> ```
>
> **TIP**: 한 번에 큰 컴포넌트를 만들기보다 작은 단위로 나눠서 요청하면 품질이 높아집니다.

### 프롬프트 기록

> 개발 중 사용한 AI 프롬프트를 기록하세요. 심사 시 가산점이 될 수 있습니다.

#### 프롬프트 1
- **목적**: (무엇을 만들려고 했는지)
- **사용 도구**: Claude Code
- **프롬프트 내용**:
```
(실제 사용한 프롬프트)
```
- **결과**: (어떤 결과를 얻었는지)

---

## 다음 단계

개발이 진행되면 아래 문서를 업데이트하세요:

- **마케터** → [03_marketing.md](./03_marketing.md): 개발된 **화면 구성**과 **핵심 기능**을 참고하여 콘텐츠를 기획합니다.
- **발표자** → [04_ppt.md](./04_ppt.md): 완성된 기능을 기반으로 데모 시나리오를 작성합니다.
- **전체 흐름** → [README.md](./README.md): 문서 간 관계와 타임라인을 확인합니다.
